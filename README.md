# 🚀 smart-env

A modern, zero-dependency, safe environment variable manager for Node.js.  
It supports **auto .env loading**, **schema validation**, **safe getters**, and **TypeScript typings**.

Perfect for MERN, Express, CLI tools, and scalable backend projects.

---

# ⭐ Features

- 🔄 Auto-load `.env`
- 🛡 Schema validation (required + type checking)
- ✔ Safe getters: `get()` + `must()`
- 📦 Zero dependencies
- 🧪 TypeScript typings included
- ⚡ Ultra lightweight
- 🌎 Get all environment variables
- 🔐 Prevents missing important keys

---

# 📦 Installation

Install via npm:

```sh
npm install smart-env
```
